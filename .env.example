# Fichier de configuration pour Portfolio
# Copier ce fichier en .env et ajuster les valeurs

# Django Backend
DEBUG=True
SECRET_KEY=django-insecure-portfolio-dev-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,portfolio_backend
CORS_ALLOWED_ORIGINS=http://localhost:3001

# PostgreSQL Database (Configuration unique à Portfolio)
POSTGRES_DB=portfolio_db
POSTGRES_USER=portfolio_user
POSTGRES_PASSWORD=portfolio_password

# Frontend React
REACT_APP_API_URL=http://localhost:8001

# NOTES:
# - Tous les noms utilisent le préfixe "portfolio_" pour éviter les conflits
# - Les ports sont différents d'investment_game (3001 vs 3000, 8001 vs 8000, 5433 vs 5432)
# - Cette configuration est totalement isolée des autres projets
